replicaCount: 1

image:
  repository: ghcr.io/darshjme/darshjdb
  pullPolicy: IfNotPresent
  tag: "latest"

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 7700

ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: darshjdb.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources:
  limits:
    cpu: 1000m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

postgresql:
  enabled: true
  auth:
    username: darshan
    # Override in production: --set postgresql.auth.password=<secure-value>
    password: darshan
    database: darshjdb
  primary:
    persistence:
      size: 10Gi

env:
  RUST_LOG: info
  DDB_PORT: "7700"
  DDB_ADMIN_DIR: /usr/share/darshan/admin

secrets:
  # REQUIRED: set via --set secrets.jwtSecret=<secure-value> (minimum 32 chars)
  jwtSecret: ""  # Empty forces override — Helm template must reject blank values

nodeSelector: {}
tolerations: []
affinity: {}
