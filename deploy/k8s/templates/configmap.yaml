apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "darshandb.fullname" . }}-config
  labels:
    {{- include "darshandb.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
