apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "darshjdb.fullname" . }}-config
  labels:
    {{- include "darshjdb.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
