apiVersion: v1
kind: Service
metadata:
  name: {{ include "darshandb.fullname" . }}
  labels:
    {{- include "darshandb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "darshandb.selectorLabels" . | nindent 4 }}
