apiVersion: v1
kind: Service
metadata:
  name: {{ include "darshjdb.fullname" . }}
  labels:
    {{- include "darshjdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "darshjdb.selectorLabels" . | nindent 4 }}
